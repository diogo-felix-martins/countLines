This program counts the lines of files in this directory.

---

Usage example:

    countLines .c .txt -e .exe -r

Arguments:

- include file types before -e
- exclude file types after -e
- -r also count files in subfolders
